
# This function creates a Poll object for use in creating and modifying Poll objects.
createPollObject = function(  = NULL){
object = as.list(environment()) 
 object = userObject[as.character(userObject) != "NULL"] 
 return(userObject) 
 }