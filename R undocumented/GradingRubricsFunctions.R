
# This function creates a GradingRubrics object for use in creating and modifying GradingRubrics objects.
createGradingrubricsObject = function(id  = NULL, created  = NULL, title  = NULL, total points  = NULL, realm  = NULL, realm_id  = NULL){
object = as.list(environment()) 
 object = userObject[as.character(userObject) != "NULL"] 
 return(userObject) 
 }