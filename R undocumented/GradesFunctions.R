
# This function creates a Grades object for use in creating and modifying Grades objects.
createGradesObject = function(enrollment_id  = NULL, assignment_id  = NULL, section_id  = NULL, grade  = NULL, comment  = NULL, comment_status  = NULL, timestamp  = NULL){
object = as.list(environment()) 
 object = userObject[as.character(userObject) != "NULL"] 
 return(userObject) 
 }