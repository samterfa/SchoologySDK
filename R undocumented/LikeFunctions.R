
# This function creates a Like object for use in creating and modifying Like objects.
createLikeObject = function(  = NULL){
object = as.list(environment()) 
 object = userObject[as.character(userObject) != "NULL"] 
 return(userObject) 
 }