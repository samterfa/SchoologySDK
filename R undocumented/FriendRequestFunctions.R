
# This function creates a FriendRequest object for use in creating and modifying FriendRequest objects.
createFriendrequestObject = function(requestor_id  = NULL, requester_name  = NULL, picture_url  = NULL, school_name  = NULL, school_id  = NULL, created  = NULL){
object = as.list(environment()) 
 object = userObject[as.character(userObject) != "NULL"] 
 return(userObject) 
 }