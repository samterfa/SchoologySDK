
# This function creates a Role object for use in creating and modifying Role objects.
createRoleObject = function(title  = NULL, faculty  = NULL){
object = as.list(environment()) 
 object = userObject[as.character(userObject) != "NULL"] 
 return(userObject) 
 }