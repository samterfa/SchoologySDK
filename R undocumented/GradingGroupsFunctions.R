
# This function creates a GradingGroups object for use in creating and modifying GradingGroups objects.
createGradinggroupsObject = function(id  = NULL, title  = NULL, section_id  = NULL, members  = NULL){
object = as.list(environment()) 
 object = userObject[as.character(userObject) != "NULL"] 
 return(userObject) 
 }