
# This function creates a MediaAlbumComments object for use in creating and modifying MediaAlbumComments objects.
createMediaalbumcommentsObject = function(content_id  = NULL, uid  = NULL, comment  = NULL, created  = NULL){
object = as.list(environment()) 
 object = userObject[as.character(userObject) != "NULL"] 
 return(userObject) 
 }