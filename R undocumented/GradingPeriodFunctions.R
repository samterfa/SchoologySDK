
# This function creates a GradingPeriod object for use in creating and modifying GradingPeriod objects.
createGradingperiodObject = function(title  = NULL, start  = NULL, end  = NULL){
object = as.list(environment()) 
 object = userObject[as.character(userObject) != "NULL"] 
 return(userObject) 
 }