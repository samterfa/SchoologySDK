
# This function creates a Submissions object for use in creating and modifying Submissions objects.
createSubmissionsObject = function(revision_id  = NULL, created  = NULL, late  = NULL, num_items  = NULL, draft  = NULL, body  = NULL){
object = as.list(environment()) 
 object = userObject[as.character(userObject) != "NULL"] 
 return(userObject) 
 }