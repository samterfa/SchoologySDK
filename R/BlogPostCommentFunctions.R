
# This function creates a BlogPostComment object for use in creating and modifying BlogPostComment objects.
createBlogpostcommentObject = function(comment  = NULL, uid  = NULL){
object = as.list(environment()) 
 object = userObject[as.character(userObject) != "NULL"] 
 return(userObject) 
 }