
# This function creates a GradingScales object for use in creating and modifying GradingScales objects.
createGradingscalesObject = function(id  = NULL, title  = NULL, type  = NULL, auto_averaging  = NULL, hide_numeric  = NULL){
object = as.list(environment()) 
 object = userObject[as.character(userObject) != "NULL"] 
 return(userObject) 
 }