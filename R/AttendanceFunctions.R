
# This function creates a Attendance object for use in creating and modifying Attendance objects.
createAttendanceObject = function(enrollment_id  = NULL, date  = NULL, status  = NULL, comment  = NULL){
object = as.list(environment()) 
 object = userObject[as.character(userObject) != "NULL"] 
 return(userObject) 
 }