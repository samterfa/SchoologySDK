
# This function creates a Enrollment object for use in creating and modifying Enrollment objects.
createEnrollmentObject = function(id  = NULL, uid  = NULL, admin  = NULL, status  = NULL){
object = as.list(environment()) 
 object = userObject[as.character(userObject) != "NULL"] 
 return(userObject) 
 }