
# This function creates a Search object for use in creating and modifying Search objects.
createSearchObject = function(uid  = NULL, name  = NULL, picture_url  = NULL, school  = NULL, school_id  = NULL){
object = as.list(environment()) 
 object = userObject[as.character(userObject) != "NULL"] 
 return(userObject) 
 }